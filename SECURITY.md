# Security

Do not report real credentials, `.env` contents, private repository source, or access tokens in a public GitHub issue.

For a security issue, use the private security-reporting mechanism configured on the repository once the public repo is live.

Repo Backup intentionally treats suspected secrets conservatively and may block a backup rather than risk pushing sensitive content.
