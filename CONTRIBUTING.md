# Contributing

Repo Backup is safety-sensitive Git tooling.

Before proposing code changes:

- preserve the no-force-push / no-auto-merge guarantees
- keep new discovery opt-in
- prefer explicit blocks over guessing
- include a Windows PowerShell 5.1 parse check
- never add tests that require real user repositories or secrets

For public beta, open an issue before large architectural changes.
