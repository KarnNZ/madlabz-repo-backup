# Demo

Run:

```bash
repo-backup demo
```

The demo is intentionally fake/read-only. It does not:

- scan your disks
- read repository contents
- change Repo Backup configuration
- call Git
- commit
- push
- touch Windows Task Scheduler

It exists so a new user can understand the product before onboarding.
